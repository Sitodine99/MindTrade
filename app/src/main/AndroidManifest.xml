<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindTrade"
        tools:targetApi="31">

    <activity
        android:name=".EmotionsActivity"
        android:exported="false" />

    <!-- LoginActivity como punto de entrada inicial -->
    <activity
        android:name=".LoginActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity
        android:name=".RegisterActivity"
        android:exported="false" />

    <!-- WelcomeActivity para dar la bienvenida tras el registro -->
    <activity
        android:name=".WelcomeActivity"
        android:exported="false" />

    <!-- TradingStyleActivity para seleccionar estilo de trading -->
    <activity
        android:name=".TradingStyleActivity"
        android:exported="false" />

    <!-- MainActivity como pantalla principal para usuarios autenticados -->
    <activity
        android:name=".MainActivity"
        android:exported="true" />

</application>

    </manifest>
