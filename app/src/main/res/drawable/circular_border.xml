<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo blanco con borde -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/white"/>
            <size android:width="120dp" android:height="120dp"/> <!-- Tamaño del borde y fondo circular -->
        </shape>
    </item>

    <!-- Imagen circular interna -->
    <item android:right="4dp" android:left="4dp" android:top="4dp" android:bottom="4dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/black"/> <!-- Color de relleno interno (opcional, se oculta con la imagen) -->
        </shape>
    </item>
</layer-list>
