<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/torosyosos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/toros_y_osos"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/torosyosos" />

    <!-- Contenido Principal -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/mainContentLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Barra Superior -->
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/black"
                app:contentInsetStart="16dp"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
                app:titleTextColor="@android:color/white">

                <!-- Contenedor Principal -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="8dp">

                    <!-- Iconos con Avatar -->
                    <LinearLayout
                        android:layout_width="1dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="horizontal"
                        android:gravity="center">

                        <!-- Avatar -->
                        <com.google.android.material.imageview.ShapeableImageView
                            android:id="@+id/userAvatar"
                            android:layout_width="36dp"
                            android:layout_height="36dp"
                            android:layout_marginEnd="16dp"
                            android:contentDescription="Avatar principal"
                            android:src="@drawable/interrogacion"
                            app:shapeAppearanceOverlay="@style/CircularImage"
                            app:strokeColor="@android:color/white"
                            app:strokeWidth="1dp"
                            android:padding="2dp"
                            android:scaleType="fitCenter" />

                        <com.google.android.material.imageview.ShapeableImageView
                            android:id="@+id/tradingStyleImage"
                            android:layout_width="36dp"
                            android:layout_height="36dp"
                            android:layout_marginEnd="16dp"
                            android:contentDescription="Estilo de trading"
                            android:src="@drawable/interrogacion"
                            app:shapeAppearanceOverlay="@style/CircularImage"
                            app:strokeColor="@android:color/white"
                            app:strokeWidth="1dp"
                            android:padding="2dp"
                            android:scaleType="fitCenter" />

                        <com.google.android.material.imageview.ShapeableImageView
                            android:id="@+id/psicoImage"
                            android:layout_width="36dp"
                            android:layout_height="36dp"
                            android:layout_marginEnd="16dp"
                            android:contentDescription="Estado psico"
                            android:src="@drawable/interrogacion"
                            app:shapeAppearanceOverlay="@style/CircularImage"
                            app:strokeColor="@android:color/white"
                            app:strokeWidth="1dp"
                            android:padding="2dp"
                            android:scaleType="fitCenter" />

                        <com.google.android.material.imageview.ShapeableImageView
                            android:id="@+id/emotionImage"
                            android:layout_width="36dp"
                            android:layout_height="36dp"
                            android:contentDescription="Emoción"
                            android:src="@drawable/interrogacion"
                            app:shapeAppearanceOverlay="@style/CircularImage"
                            app:strokeColor="@android:color/white"
                            app:strokeWidth="1dp"
                            android:padding="2dp"
                            android:scaleType="fitCenter" />

                    </LinearLayout>

                    <!-- Botón de Notificación -->
                    <ImageButton
                        android:id="@+id/notificationButton"
                        android:layout_width="40dp"
                        android:layout_height="40dp"
                        android:layout_marginStart="8dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="Notificaciones"
                        android:src="@drawable/ic_notifications" />
                </LinearLayout>
            </androidx.appcompat.widget.Toolbar>
        </com.google.android.material.appbar.AppBarLayout>

        <!-- Contenedor de Fragmentos -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/appbarLayout"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <LinearLayout
            android:id="@+id/accountHeaderLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="8dp"
            android:gravity="center_vertical"
            android:layout_marginTop="10dp"
            app:layout_constraintTop_toBottomOf="@id/appbarLayout">

            <TextView
                android:id="@+id/accountsSummary"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="Mis Cuentas"
                android:layout_marginStart="10dp"
                android:textSize="25sp"
                android:textStyle="bold"
                android:textColor="@color/black"
                android:gravity="center_vertical"
                android:minHeight="48dp"
                android:autoSizeTextType="uniform"
                android:autoSizeMinTextSize="14sp"
                android:autoSizeMaxTextSize="25sp"
                android:autoSizeStepGranularity="1sp" />

            <!-- Botón de Cambiar Cuentas -->
            <ImageButton
                android:id="@+id/changeAccountsButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginEnd="40dp"
                android:src="@drawable/ic_change_accounts"
                android:contentDescription="Cambiar cuentas"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:backgroundTint="#000000"
                android:scaleType="fitCenter"
                android:padding="4dp" />

            <!-- Botón de Añadir Cuenta -->
            <ImageButton
                android:id="@+id/addAccountButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/ic_add"
                android:backgroundTintMode="src_in"
                android:backgroundTint="#000000"
                android:contentDescription="Añadir estrategia"
                android:scaleType="fitCenter"
                android:padding="4dp"
                android:layout_marginEnd="40dp"/>

        </LinearLayout>



        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/accountsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/accountHeaderLayout" />



        <LinearLayout
            android:id="@+id/strategyHeaderLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="8dp"
            android:gravity="center_vertical"
            android:layout_marginTop="10dp"
            app:layout_constraintTop_toBottomOf="@+id/accountsRecyclerView">

            <TextView
                android:id="@+id/tradingStrategiesSummary"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:layout_marginStart="10dp"
                android:text="Estrategias"
                android:textSize="25sp"
                android:textStyle="bold"
                android:textColor="@color/black"
                android:gravity="center_vertical"
                android:minHeight="48dp"
                android:autoSizeTextType="uniform"
                android:autoSizeMinTextSize="14sp"
                android:autoSizeMaxTextSize="25sp"
                android:autoSizeStepGranularity="1sp" />

            <!-- Botón de Buscar -->
            <ImageButton
                android:id="@+id/searchStrategyButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginEnd="40dp"
                android:background="@drawable/ic_search"
                android:backgroundTintMode="src_in"
                android:backgroundTint="#000000"
                android:contentDescription="Buscar estrategia" />

            <!-- Botón de Añadir Estrategias -->
            <ImageButton
                android:id="@+id/addStrategyButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/ic_add"
                android:backgroundTintMode="src_in"
                android:backgroundTint="#000000"
                android:contentDescription="Añadir estrategia"
                android:scaleType="fitCenter"
                android:padding="4dp"
                android:layout_marginEnd="40dp"/>
        </LinearLayout>



        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/strategiesWithImagesRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_margin="0dp"
            app:layout_constraintTop_toBottomOf="@+id/strategyHeaderLayout" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/strategiesRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/strategiesWithImagesRecyclerView" />



    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Menú Lateral -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/nav_menu" />
</androidx.drawerlayout.widget.DrawerLayout>



